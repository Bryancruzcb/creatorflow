# CreatorFlow stress fixtures

Generated by `scripts/generate-stress-fixtures.mjs`.

## Executable fixtures

- `broken/` contains six deliberately unsafe or malformed project records. A successful test means CreatorFlow detects and contains the expected failure before rendering.
- `multi-file-package/` contains one glTF root, two binary buffers, and twenty-four nested texture dependencies.
- `non3d/terrain_8k.png` is a valid 8192 × 8192 RGB PNG. Its repetitive pixels keep transfer size reasonable while preserving the real decoded texture dimensions.
- `non3d/environment_master_8k.psd` is a valid 8192 × 8192, three-channel Photoshop file using deterministic PackBits/RLE compression.
- `non3d/ambisonic_6ch_96khz_24bit.wav` is a valid 30-second, six-channel, 96 kHz, 24-bit PCM WAV.
- `non3d/prores_4k_2s.mov` is a valid two-second 3840 × 2160 ProRes 422 Proxy smoke fixture generated locally with FFmpeg.
- `non3d/font-family/` contains six real IBM Plex Sans WOFF2 weights.
- `non3d/dense_environment_grid.fbx` is a valid FBX 7.4 ASCII mesh containing 640,000 vertices and 638,401 procedural polygons.

## Capacity-only profiles

`capacity-profiles.json` also describes PSD, ProRes, production font-family, BLEND, and FBX workloads that are intentionally not bundled. They are planning inputs only and must never receive executable pass/fail results.

The fixtures are deterministic and contain no user project data.
